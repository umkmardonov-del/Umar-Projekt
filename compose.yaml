services:
  postgres:
    env_file: ".env.docker"
    image: postgres:18
    ports:
      - "127.0.0.1:55432:5432"
    volumes:
      - pg_data:/var/lib/postgresql
      - ./db/backup.sql:/docker-entrypoint-initdb.d/backup_20_11_25.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 5s
      timeout: 3s
      retries: 3

  server:
    env_file: ".env.docker"
    develop:
      watch:
        - action: sync
          path: ./app
          target: /app/app
          initial_sync: true
          ignore:
            - __pycache__/
            - "*.pyc"
            - "**/.pytest_cache/**"
            - "**/.venv/**"
            - "**/node_modules/**"
        - action: rebuild
          path: ./pyproject.toml
        - action: rebuild
          path: ./requirements.txt
    command: ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "127.0.0.1:8000:8000"
    depends_on:
      postgres:
        condition: service_healthy

volumes:
  pg_data: